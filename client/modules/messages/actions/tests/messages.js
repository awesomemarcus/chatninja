const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../messages';

describe('messages.actions.messages', () => {
  it('should do something');
});
