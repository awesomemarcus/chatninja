const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../uploads';

describe('uploads.actions.uploads', () => {
  it('should do something');
});
